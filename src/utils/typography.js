import Typography from 'typography';

const typography = new Typography({
  baseFontSize: '18px',
  baseLineHeight: 1.666,
  googleFonts: [{ name: 'Anonymous Pro', styles: [700] }],
  headerFontFamily: ['Anonymous Pro', 'monospace'],
  bodyFontFamily: ['Anonymous Pro', 'monospace'],
});

export default typography;
